{
  "entities": {
    "Message": {
      "title": "Message",
      "description": "A community chat message",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "text": { "type": "string" },
        "userId": { "type": "string" },
        "userName": { "type": "string" },
        "userAvatar": { "type": "string" },
        "timestamp": { "type": "number" },
        "isAdmin": { "type": "boolean" },
        "isAI": { "type": "boolean" }
      },
      "required": ["text", "userId", "userName", "timestamp"]
    },
    "AppConfig": {
      "title": "App Configuration",
      "description": "Global application settings",
      "type": "object",
      "properties": {
        "status": { "type": "string", "enum": ["online", "offline"] },
        "message": { "type": "string" },
        "welcomeSettings": { "type": "object" },
        "validInviteCode": { "type": "string" },
        "paymentMethods": { "type": "array" }
      }
    }
  },
  "firestore": {
    "/messages/{messageId}": {
      "schema": "Message",
      "description": "Collection of community chat messages"
    },
    "/configs/app_status": {
      "schema": "AppConfig",
      "description": "Global app status and settings"
    }
  }
}
